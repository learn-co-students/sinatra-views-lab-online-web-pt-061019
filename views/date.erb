<!DOCTYPE html>
<html>
  <body>
  <% d = DateTime.now %>
    <h1>Today</h1>
    <p><%= "The date is #{d.strftime('%A')}, #{d.strftime('%B')} #{d.strftime('%-d')}, #{d.strftime('%Y')}." %></p>
  </body>
</html>
